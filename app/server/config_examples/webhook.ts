import { WebHookConfig } from '../modules/webhook/types';

export default {
	someName: async (_modules) => {
		// Do something
	},
	otherName: async (_modules) => {
		// Do something else
	},
} as WebHookConfig;
